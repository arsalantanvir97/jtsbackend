let mongoose;_fac‍.x([["default",()=>_fac‍.o]]);_fac‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const MONGO_URL = process.env.MONGO_URL;
const MONGO_DB = process.env.MONGO_DB;

const connectDB = async () => {
  try {
    await mongoose.connect(process.env.MONGO_URI, {
      useNewUrlParser: true,
      useUnifiedTopology: true,
      useCreateIndex: true,
      useFindAndModify: false,
    });
    _fac‍.g.console.log("\u001b[" + 34 + "m" + `Connected to Database` + "\u001b[0m");
  } catch (error) {
    _fac‍.g.console.error(error.message);
    // exit process with failure
    process.exit(1);
  }
};
_fac‍.d(connectDB);