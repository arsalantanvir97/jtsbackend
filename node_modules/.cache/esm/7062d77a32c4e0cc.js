let multer,uuidv4;_32c‍.x([["fileStorage",()=>fileStorage],["fileFilter",()=>fileFilter]]);_32c‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_32c‍.w("uuid",[["v4",["uuidv4"],function(v){uuidv4=v}]]);


       const fileStorage = multer.diskStorage({
  destination: function (req, file, cb) {
    cb(null, 'uploads')
  },
  filename: function (req, file, cb) {
    let extension = file.originalname.split('.').pop()
    cb(null, uuidv4() + '.' + extension)
  },
})

       const fileFilter = (req, file, cb) => {
  _32c‍.g.console.log('file', file)
  if (file.fieldname === '_image') {
    console.log('block1')
    if (file.mimetype.includes('image/')) {
      console.log('block2')

      cb(null, true)
    } else {
      cb(null, false)
    }
  } else {
    cb(null, false)
  }
}
