let nodemailer;_fac‍.x([["default",()=>_fac‍.o]]);_fac‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);

var smtpConfiq = {
  service: "Gmail",
  auth: {
    user: 'noreplydummy125@gmail.com',
    pass: 'kqswulhtckzdcdna',
  },
};


const  generateEmail= async (email, subject, html) => {
    try {
      const transporter = nodemailer.createTransport(smtpConfiq);
      const mailOptions = {
        from: 'arsalantanvir@yahoo.com',
        to: email,
        subject,
        text: "hiiii",
        html,
      };
      const res = await transporter.sendMail(mailOptions);
      _fac‍.g.console.log(res);
      return true;
    } catch (err) {
      _fac‍.g.console.log("err in generate email: ", err);
      return true;
    }
  }

  

_fac‍.d(generateEmail);